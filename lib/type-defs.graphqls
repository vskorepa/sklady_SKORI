type Query {
  singleRow(id: Int!): Row!
  multipleRows(storage: String!): [Row!]!
}

type Mutation {
  AddSingleRow(
    id: Int!
    name: String!
    code: String!
    description: String
    count: Int!
  ): Row!
  deleteSingleRow(id: Int!): Row!
  editCount(id: Int!, count: Int!): Row!
}
